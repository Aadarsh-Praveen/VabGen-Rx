# Production values are set as Vercel project environment variables
# (Project Settings → Environment Variables), not committed here —
# they point at the Render service URLs, which can change if a service
# is ever renamed/recreated.
#
# VITE_API_BASE_URL=https://vabgenrx-api.onrender.com
# VITE_AGENT_BASE_URL=https://vabgenrx-agent.onrender.com
